<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.thelovecats.br.adivinha">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/adivinha"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/adivinha"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Adivinha"
        tools:targetApi="m">
        <activity
            android:name=".activitys.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activitys.GameActivity">
        </activity>


        <activity android:name=".activitys.DefinicoesActivity">
        </activity>

        <activity android:name=".activitys.ListaDicaActivity">
        </activity>

        <activity android:name=".activitys.ResultadoActivity">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>